/**
 * 定义接口路径
 */
// const ENV = process.env.NODE_ENV
// const apiUrl = ENV === 'development'
//   ? '/api/' // 开发环境地址 已起代理
//   : ENV === 'production'
//     ? '/'
//     : 'https://debug.url.com'

// 接口地址
const path = {
  getVolumeList: 'http://39.96.69.79:8081/luoo/volume/list' // 期刊接口

}

export default path
